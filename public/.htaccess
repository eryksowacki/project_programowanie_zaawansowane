<IfModule mod_rewrite.c>
    RewriteEngine On
	
	RewriteBase /project_programowanie_zaawansowane/public/

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>