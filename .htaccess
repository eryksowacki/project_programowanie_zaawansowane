<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /project_programowanie_zaawansowane/

    # jeśli nie wchodzisz już do /public, to dorzuć public/
    RewriteCond %{REQUEST_URI} !^/project_programowanie_zaawansowane/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>